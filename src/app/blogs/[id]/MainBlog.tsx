'use client';

import { ArticlesItem } from '@/src/shared/types';

export const MainBlog = ({ article }: { article: ArticlesItem }) => {
	return (
		<main className=' max-w-[800px] mx-auto '>
			<div>main</div>
		</main>
	);
};
